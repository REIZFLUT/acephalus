import type { ElementType } from '@/types';

// Default built-in element types
export const DEFAULT_ELEMENT_TYPES: ElementType[] = ['text', 'media', 'html', 'json', 'xml', 'svg', 'katex', 'wrapper', 'reference'];

